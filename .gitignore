# Python 기본
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd

# 가상환경
.venv/
venv/
env/
ENV/

# OS / 에디터 부산물
.DS_Store
Thumbs.db

# VS Code
.vscode/

# 테스트 / 캐시
.pytest_cache/
.mypy_cache/

# 데이터 / 로그 (필요 시)
# 실제 학습 데이터는 상황에 따라 제외
# data/raw/
# data/processed/

# 모델 체크포인트 (나중 대비)
# *.pt
# *.bin
# *.ckpt

# HuggingFace 캐시 (로컬만)
.huggingface/# Python
__pycache__/
*.pyc

# Mac
.DS_Store

# Data (Excluded large/generated files)
data/processed/dm/
models/

# GCP credentials keys (if any JSONs are dropped here)
*.json
!data/processed/*.jsonl
!data/processed/dm/*.jsonl
# But wait, we want to exclude generated DM clips?
# User said "data/DM/v1" is what I touched.
# Actually, let's look at the structure.
# I want to IGNORE the split files: data/processed/dm/train.jsonl etc.
# But I want to KEEP the config?
# Config is python code.

# Ignore all json key files for security
*key.json
*credentials.json

# Ignore local env settings
.env
